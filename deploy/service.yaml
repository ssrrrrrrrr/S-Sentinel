apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: slo-rollout
  labels:
    app: demo-app
spec:
  type: NodePort
  selector:
    app: demo-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
