# Legacy demo-app manifests

This directory contains early standalone Kubernetes manifests used during the initial demo phase.

These files are kept only for historical reference.

Current GitOps-managed manifests are maintained under:

- `deploy/base/analysis.yaml`
- `deploy/base/rollout.yaml`
- `deploy/base/servicemonitor.yaml`
- `deploy/base/service.yaml`

Do not use the manifests in this directory as the current deployment source.
